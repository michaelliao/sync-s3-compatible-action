name: "S3-compatible Sync Action"
description: "Sync a directory to an S3-compatible (R2, OSS, COS, BOS) storage"
author: michaelliao
runs:
  using: docker
  image: Dockerfile
branding:
  icon: refresh-cw
  color: green
